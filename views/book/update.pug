extends ../layout

block content
  h1 Update Book
  include error
  form(method="POST")
    include form
    p
      input(type='submit' value='Update Book')
    p
      a.button(href='/books') Cancel
    p
      a.button(href='/books') Delete Book
