extends ../layout

block content
  h1 New Book
  include error
  form(method="POST")
    include form
    p
      input(type='submit' value='Create New Book')
    p
      a.button(href='/books') Cancel
